using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Consigna : MonoBehaviour
{
    /*
    Crear una clase llamada “SimpleList<T>” que implemente la interfaz “ISimpleList<T>”
   incluida en la carpeta de este enunciado, y maneje internamente un arrray de T.

   ● Desarrollar todas las funciones incluidas en la misma, y hacer un override de la
   función ToString() heredada de Object, que devuelva un string con todos los
   elementos que contenga SimpleList pasados a string, separados por coma.

   ● Desarrollar una escena en Unity que permita testear cada función mediante botones
   en UI y ver el contenido de una SimpleList en tiempo real sin necesidad de usar las
   ventanas Inspector ni Console.

   ● A contemplar:
       ○ En caso de que el array interno se complete, deberá reemplazarlo con otro
         del doble de tamaño y copiar los datos del original al nuevo.
    */
}
